# API Configuration
# Use relative path for production builds (works with Caddy reverse proxy)
# Vite dev server will proxy /api requests to localhost:8080 via vite.config.ts
VITE_API_URL=/api/v1

# For production, update to your actual API URL
# VITE_API_URL=https://api.yourdomain.com/api/v1
